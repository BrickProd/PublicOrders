<UserControl x:Class="PublicOrders.Views.WinnersPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PublicOrders.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid>
        <TabControl>
            <TabItem Header="Избранное" Style="{StaticResource PanelContentStyleMini}">
                <ListView Margin="10" BorderThickness="1 0 0 0">
                    
                </ListView>
            </TabItem>
            <TabItem Header="Чёрный список" Style="{StaticResource PanelContentStyleMini}">
                <ListView Margin="10" BorderThickness="1 0 0 0">
                    
                </ListView>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
